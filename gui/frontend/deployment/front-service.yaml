apiVersion: v1
kind: Service
metadata:
  name: codeco-gui-frontend-service
  labels:
    app: codeco-gui-frontend
    component: frontend
spec:
  type: ClusterIP
  ports:
    - port: 3000
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app: codeco-gui-frontend
